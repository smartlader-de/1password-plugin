description = "Secure CLI and AI tool API keys with 1Password Shell Plugins and biometric auth."
prompt = """
Use the bundled 1password cli-auth skill at skills/cli-auth/SKILL.md.

Follow the repository safety rules: load references/security.md, never print API keys or injected credential values, and ask explicit confirmation before installing shell plugins, editing shell config, deleting plaintext originals, modifying MCP config files, or configuring browser-agent credential handoff.
"""
