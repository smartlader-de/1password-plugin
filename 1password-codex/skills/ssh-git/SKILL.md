---
name: ssh-git
description: Use when handling 1Password SSH/Git: keys, GitHub/GitLab registration, commit signing, or SSH agent access.
---

# 1Password SSH and Git for Codex

Load `../../../skills/ssh-git/SKILL.md` from the repository root and follow it.
Do not continue from this wrapper alone.

Never print private key material; ask explicit confirmation before writing
keys, provider accounts, `~/.ssh/config`, `.gitconfig`, or `.git/config`.
